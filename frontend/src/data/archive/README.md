/**
 * ARCHIVED — not imported by the app.
 * Former local catalogue fallbacks used before / alongside early Supabase snapshots.
 * Live data: Supabase `catalogue_snapshots` (+ related catalogue tables via import).
 */
